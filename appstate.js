const state = {
  mode: 'COMBINATION',
  activeBank: 'A',
  patchName: 'Initial',
  patchNumber: 0,
};
const zeroOneAppErrors = {
  midiErrors: [],
};
// loadState('wfdState');
let patchNumberCounter = 0;
